services:
  personal-site:
    image: registry.cn-hangzhou.aliyuncs.com/azurexh/personal-site:latest
    restart: unless-stopped
    environment:
      - VIRTUAL_HOST=mkt77.site
      - VIRTUAL_PORT=3000
    networks:
      - web

networks:
  web:
    external: true
